process StandAloneMuonProduction = {

source = PoolSource{
# 	untracked vstring fileNames ={'file:/afs/cern.ch/cms/Physics/muon/CMSSW/DT/samples/2006-06-13/MuonRecHit.root'}
 	untracked vstring fileNames ={'file:/afs/cern.ch/cms/Physics/muon/CMSSW/DT/samples/2006-06-16/MuMinusSeg20GeV.root'}
 		             untracked int32 maxEvents = 1000
 		 	     untracked uint32 debugVebosity = 10
  			     untracked bool   debugFlag     = true
 			   }

	service = MessageLogger{
	 			untracked vstring destinations = {"cout","debug.txt"}
				untracked PSet debug.txt = {
					untracked string thresold = "DEBUG"
					untracked PSet INFO = {untracked int32 limit = 0 }	
					untracked PSet DEBUG = {untracked int32 limit = 0 }	

					}
				untracked vstring debugModules = {"*"}
      			        untracked vstring categories = { "FwkJob" }
			        untracked PSet cout = 
      				  { 
				untracked string thresold = "DEBUG"
				untracked PSet INFO = {untracked int32 limit = 0 }	
				untracked PSet DEBUG = {untracked int32 limit = 0 }	

 			         untracked PSet default = { untracked int32 limit = 0 }    # kill all messages in the log
	       	 		 untracked PSet FwkJob  = { untracked int32 limit = -1 }  # except *all* of FwkJob's	  
      				  }
      
      untracked vstring fwkJobReports = {"FrameworkJobReport.xml"}

	}

        include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

	include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi" 

	## DT Geometry
#        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
#        include "Geometry/DTGeometry/data/dtGeometry.cfi"
	## CSC Geometry
#	include "Geometry/CSCGeometry/data/cscGeometry.cfi"

	## ES producer
	include "RecoMuon/DetLayers/data/muonDetLayerGeometry.cfi"
	include "Geometry/CommonDetUnit/data/globalTrackingGeometry.cfi"

#----------------------------------------------------------------------------------------

	## Seed generator
	include "RecoMuon/MuonSeedGenerator/data/MuonSeedProducer.cfi"

#----------------------------------------------------------------------------------------

	## STA generator
	include "RecoMuon/StandAloneMuonProducer/data/StandAloneMuonProducer.cfi"

#----------------------------------------------------------------------------------------

	module out = PoolOutputModule {
				       untracked string fileName ="/tmp/MuonSeeds.root"
		  		      }

#------------------------------------ Analyzers ------------------------------------------


	path p = {MuonSeedProducer,standAloneMuonProducer}
#	path p = {MuonSeedProducer}

	endpath this_is_the_end = {out}
}
